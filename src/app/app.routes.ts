import { Routes } from '@angular/router';
import { FilterTableComponent } from './filter-table/filter-table.component';

export const routes: Routes = [{ path: '', component: FilterTableComponent }];
